# 开发验证结果

验证日期：2026-08-18  
验证范围：本地开发配置、Fake 北森、正式北森最近 7 天同步、Fake/正式模型、内存/正式 SMTP、Django 页面与核心业务服务。  
业务验收：未执行，由用户在部署环境自行评估。

## 已执行结果

| 命令/检查 | 结果 |
| --- | --- |
| `python manage.py check` | 通过，0 个系统检查问题 |
| `python manage.py makemigrations --check --dry-run` | 通过，无未生成迁移 |
| `python manage.py migrate --noinput` | 通过，本地 SQLite 已应用全部迁移 |
| `python manage.py test` | 通过，95 项测试全部成功 |
| `python manage.py check --deploy` | 使用临时生产安全变量执行，通过 |
| `python -m compileall` | 通过 |
| `docker compose --env-file .env.example config --quiet` | Compose 配置解析通过；本机 Docker 用户配置文件产生权限警告，不影响解析结果 |

## 测试覆盖

- 独立账号、5 次失败锁定、来源 IP 记录、60 分钟无操作退出、密码重置会话失效和初始管理员命令。
- 北森 Token 缓存、401 刷新、分页、500 重试、批量投递/职位请求体、动态字段字典、候选人多岗位投递、简历模块、文件版本和重复同步幂等。
- DOCX 解析成功、低质量文本和不支持格式失败状态。
- 岗位规则发布历史、JD 规则草稿、PII 脱敏、报告校验、批次上限、部分失败、报告复用和强制重新分析。
- 岗位配置中心的数据模型、配置状态、岗位说明三种确认方式、规则版本绑定、V2 归档 V1、北森岗位说明更新后继续使用旧规则。
- DOCX 招聘汇总和 XLSX 负责人表解析、参考资料版本发布与归档、岗位精确/别名/歧义匹配、参考资料负责人自动带出。
- HR 访问岗位配置和评估规则、配置不完整时分析与送审后端拦截、历史岗位禁用、智能差异摘要失败后继续人工确认。
- AI 规则生成遇到模型连接、超时、认证、限流或返回格式异常时，页面显示用户可理解的提示，不再暴露错误详情页。
- AI 规则生成兼容模型将推荐分数线返回为数字、数字文本、区间数组或带下限字段的对象；评分维度权重也会先转换为数字再校验，格式异常时显示业务提示而不是错误详情页。
- 岗位规则草稿使用卡片式业务表单编辑硬性要求、评分维度、加分项和推荐分数线；HR 与管理员无需查看或修改 JSON，权重和分数线填写错误时显示可理解的提示。
- 全屏处理提示增加“取消等待”；AI 岗位规则生成支持真正提交取消请求，即使模型稍后返回也不会保存草稿。模型请求最长等待默认限制为 90 秒且不重复重试，浏览器回退或恢复页面时会自动解除旧遮罩。
- 负责人审核结果页会在“已提交且审核通过”的候选人右侧显示“导入人才库”；HR 可直接导入，已在人才库的候选人显示当前状态，草稿、待讨论和不通过记录不能通过该入口导入。
- 报告页面、PDF、脱敏 Excel、管理员模型用量权限。
- 负责人 Token 哈希、HTML/纯文本邮件、可点击审核链接、校验失败保留填写、主动清空、内部结果查看、完成失效、撤销、删除撤回和重新开启保留历史。
- 负责人公开审核页可查看送审时固定的 AI 分析报告；报告展示匹配分、推荐等级、岗位要求核对、维度评分、优势、风险、信息不足和面试建议，不展示 HR 备注、模型信息、调用用量、费用或原始模型数据；重新分析不会替换原审核链接中的报告。
- 人才库唯一成员、标签、推荐、旧简历确认、移出恢复、清理和北森正式投递关联。
- 未分析投递删除、投递 3 天清理、候选人业务数据删除和 `applicationId` 排除标识。
- HR/管理员主要页面渲染和简历下载登录权限。
- 候选人详情展示北森教育经历、工作经历、技能、招聘渠道和删除入口；岗位详情展示北森 JD、招聘渠道和删除入口；岗位列表只统计可见投递。
- 岗位 JD 同名段落标题去重、岗位详情冻结控制区与投递表格内部滚动结构、候选人返回来源岗位入口。
- 北森学校空值编码过滤、候选人详情技术信息隐藏、用户可读简历名称、预览文件名及教育/工作经历上下布局。
- 站内通知和工作台通知支持安全跳转并只允许查看当前用户自己的通知。
- 北森 `recruitmentStandard` UUID 占位值不会显示为招聘标准，正常文字招聘标准仍保留。

## 正式联调结果

- 2026-08-11 已使用正式连接器 Key/Secret 成功获取 Bearer Token，有效期 7200 秒。
- 已完成 WireGuard 分流配置，本地访问北森 OpenAPI 时由腾讯云固定出口 `122.51.104.159` 发出。
- 2026-08-11 在固定连接北森真实 IP 的验证中，Token、候选人 ID、个人信息、简历模块、投递、职位、原始简历和标准简历接口均返回 HTTP 200；最近 7 天首批返回 57 个候选人 ID，两类简历接口均包含下载地址字段。
- 腾讯云出口 `122.51.104.159` 已加入北森 OpenAPI 受信 IP。
- 2026-08-12 已将 `openapi.italent.cn` 加入 Clash Verge Fake-IP 排除，域名可解析到真实地址 `124.250.100.145`。
- 2026-08-12 已在 Clash Verge 中配置绑定 `wg-windows` 的 `BEISEN-WIREGUARD` 直连出口，并将北森域名规则指向该出口。项目原生客户端的 Token 和 `Applicant/GetApplicantIdsByDate` 均返回 HTTP 200；最近 7 天当前首批返回 55 个候选人 ID，Clash 日志确认命中 `BEISEN-WIREGUARD`，WireGuard 流量同步增长。
- 2026-08-12 已执行正式最近 7 天同步任务 `6`：状态 `success`，候选人 61、投递 61、职位 3、问题 0。
- 61 条投递均已取得投递时间和招聘渠道；北森投递接口需要在请求体中显式传入 `fieldNames`，系统已配置并验证。
- 61 份简历均解析成功并生成标准 PDF；磁盘现有 122 个简历相关文件，与数据库 122 个文件引用完全一致，无缺失和孤立文件。
- 正式职位接口返回的 3 个职位 `jobType` 均为空字符串，`category/kind` 仅返回未确认含义的数字枚举，因此岗位类型暂不猜测填充，等待北森字段权限或枚举说明。
- 已将 `httpx/httpcore` 日志级别设为 `WARNING`，正式同步终端不再记录带签名的简历下载 URL。
- 北森投递时间可能是不带时区的本地时间，系统按项目时区 `Asia/Shanghai` 转为带时区时间保存。
- 2026-08-12 首次全量任务 `7` 在第 13 批个人资料处失败；根因是一条历史资料含北森未转义的 U+000B 控制字符，HTTP 仍为 200，但不符合严格 JSON。客户端已增加兼容解析回退。
- 修复后只读遍历全部 1508 个候选人 ID，完成 192 次个人资料、投递和简历模块批量请求，取得个人资料 1508 条、投递 1455 条、模块记录 6498 条，失败 0。本轮尚未自动重新下载全部历史简历文件。
- 同步页面现在即使任务级失败数为 0，也会显示任务错误原因，方便管理员诊断。
- 2026-08-13 核查首次全量任务 `8`：1508 名候选人、1455 条投递、10 个职位、1453 份简历已同步；原 11 个问题均为简历解析质量问题，其中 6 份源 PDF 文本不足 200 字、5 份源文件为 PNG/JPG。
- 11 份问题简历均存在可提取 238–794 字文本的北森标准 PDF。解析器已增加标准 PDF 自动回退，重新解析后 11/11 成功，当前 1453 份简历全部为 `success`，任务 `8` 已更新为成功且剩余问题 0。
- 2026-08-13 已使用本地全量数据库抽样验证 5 个岗位的岗位详情、候选人详情、标准 PDF 预览和源文件下载，全部返回 HTTP 200。
- HTML、PDF 和图片三类源简历均已分别验证：标准 PDF 预览返回 `application/pdf`，源文件下载返回附件响应。
- 1453 份简历的源文件和标准 PDF 均存在，无缺失文件；源文件类型为 HTML 996、PDF 452、PNG 3、JPG 2。
- 当前 1505 名候选人有教育经历模块、950 名有工作经历模块、892 名有技能信息；页面已支持展示这些北森同步字段。
- 特殊数据检查确认：1 名候选人投递 2 个岗位，候选人/投递/简历重复组均为 0；有效岗位投递均有关联简历。
- 招聘渠道原始值为北森 UUID，已使用候选人字段字典转换并回填为智联招聘 996 条、BOSS直聘 459 条。
- 无联系方式页面占位、永久删除后同步排除和招聘渠道字典转换已增加回归测试。
- HR 和管理员均可查看并发起北森同步任务。
- 同步任务问题数量可进入问题明细页，按候选人和岗位展示受影响内容、通俗原因与处理建议；页面不显示 Applicant ID 或原始技术错误，简历预览问题可直接提交重新获取任务。
- 用户已使用 HR 账号完成“最近 1 天”同步任务测试，并确认没有问题。
- “ai算法工程师”已发布 V1 临时测试规则；该岗位 10 条投递均有成功解析的简历，模型配置和测试负责人配置齐全，可由用户执行 AI 分析及送审测试。
- 2026-08-13 首次真实 AI 分析暴露费用计算类型错误：模型已正常返回，但系统将 `Decimal` 与 `float` 相乘而把任务误标为“模型服务异常”。现已统一使用 `Decimal` 计算费用，并增加首次模型版本创建场景的回归测试。
- 页面已增加统一任务提交遮罩；本地测试环境将 AI 分析、北森同步、简历补拉和送审邮件放入后台线程，避免浏览器在请求期间持续转圈。AI 任务页显示完成数量、百分比并每 3 秒刷新；同步任务页执行期间每 5 秒刷新。
- 2026-08-13 修复本地 SQLite 并发导致的 `SessionInterrupted`：模型网络请求不再被数据库事务包裹，SQLite 启用 WAL 和 30 秒忙等待，自动刷新不再逐次写会话，本地后台任务串行执行。分析任务 `#2` 已恢复并完成，3 份简历全部成功生成报告。
- 2026-08-14 首次负责人送审批次创建成功但邮件发送失败，错误为 `WinError 10013`。沙箱外验证企业邮箱 SMTP 465/587 端口及认证均正常，确认原因为本地服务进程缺少外网套接字权限；重新以具备网络权限的方式启动服务后，批次 `#1` 重发成功，3 个审核项的公开链接返回正常。
- 2026-08-14 负责人审核邮件已改为 HTML/纯文本双格式并增加明确的审核按钮；提交校验失败会保留已填结果，审核人可主动清空草稿，HR 可查看逐份审核结果。
- 2026-08-14 站内通知及工作台最新通知已增加业务直达入口；未分析投递也可软删除，岗位详情与候选人详情均显示删除入口。
- 2026-08-14 已过滤并回填北森 `recruitmentStandard` UUID 占位值，清理现有 10 个岗位，数据库剩余全零 UUID 为 0。
- 2026-08-14 已在实际浏览器中检查新版工作台、岗位列表和岗位详情；桌面浅色、桌面深色、390px 移动端和移动端折叠菜单显示正常。
- 2026-08-14 已使用真实 AI 报告检查新版报告页：总分摘要、岗位要求、维度评分、优势/风险/信息不足和移动端布局显示正常，页面不再出现原始字典文本。
- 2026-08-14 已重新生成现有 10 个岗位的展示 JD，全部岗位的“岗位职责”和“任职要求”计数均为 1；本地运行服务登录后实测岗位详情和候选人详情均返回 HTTP 200，冻结区、批量操作区、内部滚动容器和返回按钮均已出现在实际响应中。
- 2026-08-17 已完成岗位配置中心阶段 A–F：数据模型、参考资料管理、岗位匹配、岗位说明对比确认、规则生成发布、负责人配置、配置状态、运行拦截、工作台和同步入口均已实现。
- 2026-08-17 本地 SQLite 已应用 `recruitment.0002`、`analysis.0002`、`reviews.0003`；已导入 9 条当前岗位说明确认记录和 42 条正式岗位负责人关联。
- 当前 8 个有效岗位等待 HR 在界面核对并发布首版正式规则；`ai算法工程师`原 V1 保留为历史使用状态，岗位显示“需更新”，等待基于当前确认内容发布 V2。系统未自动伪造正式规则。
- 2026-08-18 AI 规则生成连接失败已定位为本地服务进程缺少外网套接字权限，底层错误为 `WinError 10013`；模型配置在沙箱外最小请求验证成功，本地 Django 服务已用具备网络权限的进程重新启动。
- 2026-08-18 真实模型曾将 `rating_thresholds.priority/review` 返回为分数区间数组，旧校验直接执行 `int(list)` 导致页面报错；现已补充真实返回形态回归测试、明确模型输出要求并兼容常见分数区间格式。
- 2026-08-14 已修复候选人学校显示 `-32767` 的问题并回填 85 名候选人，剩余异常学校占位值为 0；候选人详情不再输出 Applicant ID、解析质量和后台乱码文件名。
- HR 和管理员均可对缺少简历的北森投递发起按需补拉；后台完成文件下载、标准 PDF、解析和投递关联，并通过站内通知反馈成功或失败。
- 另有 1 条历史岗位投递没有当前简历；该候选人没有任何简历版本，且历史岗位按现有规则不主动下载简历，因此不属于本次 11 个问题。
- 项目入口已支持自动读取根目录 `.env`，且不会覆盖调用环境中已经存在的变量；北森、模型和 SMTP 配置均可由 `manage.py` 自动识别。
- 模型网关、API Key 和 `gpt-5.6-sol` 已通过模型列表与最小 JSON 对话请求验证，实际返回 `{"status":"ok"}`。
- 企业邮箱 SMTP 已完成认证和向 `HR@nuptio.net` 的测试发信；当前全部 10 个岗位已临时关联测试负责人 `test <zhangyufan@nuptio.net>`，真实岗位负责人和生产配置仍待提供。
- Docker Compose 仅完成配置解析，未在本机拉取镜像、构建或启动完整容器。
- `docker/backup/backup.sh` 已实现加密与最近 7 个恢复点轮转，但本机没有 `sh`，未执行脚本 dry-run，也未执行灾难恢复演练。
- OCR 代码路径已实现，但当前测试样本未调用本机 Tesseract 中文识别。

## 仍待验证

- 岗位名称、JD、候选人归属和个人经历仍需 HR/管理员对照北森页面完成人工业务抽查。
- 正式岗位—负责人配对、生产域名、PostgreSQL、MinIO、完整容器启动和备份恢复演练仍待完成。
- 业务验收由用户执行，本文件只记录开发和联调结果。
